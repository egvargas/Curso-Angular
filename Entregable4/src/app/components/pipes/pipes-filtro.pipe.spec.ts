import { FiltroCursosPipe } from './pipes-filtro.pipe';

describe('FiltroCursosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCursosPipe();
    expect(pipe).toBeTruthy();
  });
});
